<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.jiujiu.DAO.UserDAO">

    <!--查询所有用户-->
    <select id="selectAllUserFromUser" resultType="User">
        select * from user
    </select>
    <!--分页查询-->
    <select id="selectByPaging" resultType="User" parameterType="java.lang.Integer">
        select * from user limit #{start},#{rows}
    </select>
    <!--查询总记录数-->
    <select id="selectRecords" resultType="java.lang.Integer">
        select count(1) from user
    </select>
    <!--添加一个新用户-->
    <insert id="insertUser">
        insert into user values(#{id},#{company},#{username},#{salt},#{password},#{nickname},#{registerTime},#{status},#{address},#{phone})
    </insert>
    <!--根据uuid修改用户-->
    <update id="updateUser">
        update user set company = #{company},
                        username = #{username},
                        salt = #{salt},
                        password = #{password},
                        nickname = #{nickname},
                        registerTime = #{registerTime},
                        status = #{status},
                        address = #{address},
                        phone = #{phone}
                  where id = #{id}
    </update>
</mapper>